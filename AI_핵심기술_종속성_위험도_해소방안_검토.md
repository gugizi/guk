# AI 핵심기술 종속성 위험도 해소방안 검토

---

## □ 현황 및 문제점

○ **(핵심기술)** 시스템 플랫폼의 핵심기술인 LLM\*·RAG\*·벡터DB·지식그래프는 기술 종속성 위험도가 높은 것으로 평가됨

\* LLM(Large Language Model) : 대규모 언어모델 / RAG(Retrieval-Augmented Generation) : 검색증강생성

※ 별도 제출문서 : 기술 분석서

○ **(LLM 종속성)** 특정 기업의 상용 모델 서비스 정책 변화에 따라 응용서비스 운영이 영향을 받는 종속성 존재

○ **(RAG 종속성)** 데이터를 수치화한 임베딩\* 모델 변경 시, 기존 벡터 DB의 데이터를 다시 변환해야 하는 구조적 종속성 존재  
\* Embedding : 텍스트·문서를 벡터(수치)로 변환하는 모델  
→ 임베딩 교체 시 원문 재수집·재정제부터 재수행하거나, 인덱싱 기간 중 검색서비스 중단이 불가피하여 가용성 저하 위험 [참고1]

---

## □ 종속성 위험도 해소방안

○ **(LLM 해소방안)** 엔티스 응용프로그램과 LLM 추론 엔진 사이에 독립적인 **추상화 중계 레이어(Model-Agnostic Orchestration Layer)**\*를 구축하여 비종속 아키텍처 모델 도입 [참고1]  
\* Model-Agnostic Orchestration Layer : 응용과 실제 LLM을 분리하고, 표준 API로 모델을 중계·교체하는 계층

- **(표준 API 규격 통합)** vLLM 및 TensorRT-LLM 오픈소스 분산 추론 엔진을 기반으로, 내부 구동 LLM의 입·출력 규격을 OpenAI API 호환 포맷으로 100% 일체화
- **(모델 스와핑)** 국산 특화 LLM(Solar, EXAONE 등) 또는 개방형 초거대 모델(Llama 3.1 70B 등)로 교체 시, 애플리케이션 소스코드 수정 없이 YAML 설정 변경만으로 즉시 교체 가능

○ **(RAG 해소방안)** 임베딩 모델 변경에 따른 벡터 DB 전량 재가공 종속성을 해소하기 위해 **모듈형 데이터 파이프라인** 구축 [참고1]

- **(컴포넌트화)** AI 어시스턴트를 레고 블록형 컴포넌트로 개발하여 임베딩 엔진·벡터DB만 별도 업그레이드·교체 가능하도록 구현
- **(계층 분리)** 파운데이션 계층(모델)과 인텔리전스 계층(데이터/RAG)을 분리하고, LangChain·LlamaIndex 등 오케스트레이션 프레임워크를 미들웨어로 적용하여 결합도 저하
- **(하이브리드 처리)** 단순 질의는 경량 모델·CPU 서버, 복잡 추론은 고성능 모델·GPU 인프라로 분산 처리하여 특정 자원·기술 의존도 분산  
  → 표준 API 및 MCP\* 프로토콜 도입으로 해당 모듈만 교체하여 서비스 지속 가능한 기반 마련  
  \* MCP(Model Context Protocol) : AI 모델과 외부 도구·데이터 소스를 표준 방식으로 연결하는 프로토콜

○ **(구성계획)** 비종속 아키텍처를 3계층으로 구성

- ① 엔티스 응용프로그램 계층(Client Layer) : 홈택스·손택스·엔티스 포털·AI 에이전트 등 → 단일 표준 엔드포인트로 호출
- ② 추상화 중계 레이어 : 모델 가상화, 지능형 라우팅·Failover, 보안 가드레일, 시맨틱 캐싱 적용
- ③ 표준 추론 엔진 계층 : vLLM·TensorRT-LLM 기반 OpenAI 호환 서빙 + 모듈형 RAG 파이프라인

---

## □ AI 핵심기술 비종속 아키텍처 개념도

### AS-IS (종속 구조)

```
[홈택스·엔티스·AI 에이전트]
            │
            ▼
   [특정 상용 LLM 직접 연동]
            │
            ▼
 [임베딩 모델 ↔ 벡터DB 강결합]
```

- 모델·임베딩 교체 시 응용코드 수정 및 벡터DB 전량 재변환 필요
- 전환기간 중 검색·추론 서비스 중단 위험

### TO-BE (비종속 구조)

```
[홈택스·엔티스·AI 에이전트] ──단일 표준 API──▶
        ┌─────────────────────────────────┐
        │  추상화 중계 레이어 (Gateway)     │
        │  ·모델 가상화 / Failover         │
        │  ·가드레일 / 시맨틱 캐시         │
        └─────────────────────────────────┘
                    │
     ┌──────────────┼──────────────┐
     ▼              ▼              ▼
 [vLLM]      [TensorRT-LLM]   [모듈형 RAG]
 (EXAONE 등)   (Solar 등)    (재임베딩만 수행)
     │              │              │
     └──────────────┴──────┬───────┘
                           ▼
              [온프레미스 LLM / 벡터DB]
```

- YAML 설정만으로 모델 교체(소스코드 무수정)
- Raw·Refined Zone 보존으로 재임베딩만 수행, 신·구 인덱스 병행 후 무중단 전환

---

## 참고1 AI 핵심기술 종속성 위험도 해소 검토서

### □ LLM 종속성 위험도 해소 방안(비종속 아키텍처 모델 도입)

○ 엔티스 응용프로그램과 LLM 추론 엔진 사이에 독립적인 추상화 중계 레이어를 구축함

#### ① 엔티스 응용프로그램 계층 (Client Layer)

- 홈택스, 손택스, 엔티스 포털, AI 에이전트 등 모든 업무 시스템
- **(단일화된 호출 방식)** 하위 모델 종류·GPU 위치와 무관하게 단 하나의 표준 엔드포인트로 서비스 요청 전송

#### ② 추상화 중계 레이어 (Model-Agnostic Orchestration Layer)

- 국세 각종 응용프로그램과 실제 추론 서버 사이에 위치하는 지능형 AI 게이트웨이(Gateway)

| 주요 기능 | 기술적 구현 내용 및 효과 |
|---|---|
| 모델 가상화 (Virtualization) | 엔티스는 가상 모델명으로 요청하고, 중계 레이어가 실시간 GPU 상황에 맞춰 vLLM-Exaone-32B, TRT-LLM-Solar-10.8B, Llama 3.1 등으로 물리 매핑 |
| 지능형 라우팅 & Failover | 특정 GPU 인스턴스 장애 시, 동일 기능 대체 모델 인스턴스로 트래픽 자동 우회(Zero-downtime) |
| 보안 가드레일 (Guardrails) | 주민등록번호·계좌번호 등 민감정보(PII) 마스킹 및 국세행정 정책 위반 질의 사전 차단 필터 일괄 적용 |
| 시맨틱 캐싱 (Semantic Cache) | 종합소득세 신고기간 등 피크 시기, 동일·유사 질의는 Redis 캐시에서 밀리초 단위 응답 → GPU 부하 대폭 절감 |

#### ③ 표준 API 규격 통합 (vLLM & TensorRT-LLM)

- vLLM과 TensorRT-LLM은 오픈소스 진영 및 엔비디아의 산업 표준(De-facto Standard) 고성능 분산 추론 엔진으로, 두 엔진 모두 OpenAI API Specification을 내장
- **(100% 규격 일체화의 의미)** EXAONE 기반 32B를 vLLM으로 서빙하든, Llama 기반 70B를 TensorRT-LLM으로 서빙하든, 클라이언트가 수신하는 JSON 데이터 구조가 동일함

| 구분 | vLLM (Virtual LLM) | TRT-LLM (TensorRT-LLM) |
|---|---|---|
| 개발 주체 | 오픈소스 커뮤니티(UC 버클리 중심) | NVIDIA(엔비디아 공식) |
| 최대 장점 | 사용이 쉽고, 다양한 모델을 즉시 지원 | 엔비디아 GPU 하드웨어 성능을 한계까지 활용 |
| 사용 환경 | 일반 테스트, 빠른 프로토타입 서빙 | 엔터프라이즈급 대규모 서비스, 고속 추론 환경 |

---

### □ RAG 종속성 해소 방안(모듈형 데이터 파이프라인 구축)

○ 임베딩 모델 변경에 따라 기존 벡터 DB 데이터를 모두 재가공해야 하는 구조적 종속성 해결을 위해 모듈형 데이터 파이프라인 구축

#### ① 레고 블록 형태의 컴포넌트 개발

- AI 어시스턴트를 컴포넌트 방식으로 개발하여 특정 임베딩 엔진이나 벡터DB만 별도 업그레이드·교체 가능하도록 구현

| 데이터 파이프라인 — 단계 분리 구성 |
|---|
| 원천데이터 → 파싱 → 분류 → 청킹 → **임베딩** → **인덱스 적재(벡터DB)** |
| ※ 파싱·분류·청킹 : 임베딩 모델 교체 시 **100% 재사용** / 임베딩·인덱스 : **재생성 대상 단계만** 수행 |

#### ② 데이터 관리 계층의 분리

- 파운데이션 계층(모델)과 인텔리전스 계층(데이터/RAG)을 분리하고, LangChain·LlamaIndex 등 오케스트레이션 프레임워크를 미들웨어로 사용하여 데이터 소스와 AI 모델 간 결합도 낮춤

#### ③ 하이브리드 아키텍처

- 단순·정형 질의 → 경량 모델(8B급) + CPU 서버  
- 복잡·심층 추론 → 고성능 모델(70B급) + GPU 인프라  
- 표준 API 및 MCP 프로토콜 도입으로 해당 모듈만 교체하여 서비스 지속

---

### □ 모델 중립 데이터 관리 설계의 장점

| 위험 요소 | 기존 방식 문제점 | 모델 중립 구조 설계 |
|---|---|---|
| 원문 재수집·재정제 위험 | 임베딩 교체 시 초기 파싱·전처리 과정 전반을 처음부터 재수행 | 데이터레이크 내 Raw Zone(원문·메타데이터) 및 Refined Zone(정제데이터) 완벽 보존 → 원문 재수집 없이 **재임베딩만** 수행 |
| 전처리 파이프라인 갱신 | 전처리·파싱 코드를 다시 프로그래밍해야 하는 오버헤드 발생 | 파싱·분류·청킹 독립 단계를 모듈로 완전 분리하여 기존 정제 산출물 전량 재활용 |
| 전환기간 중 가용성 중단 | 인덱싱 새로 생성하는 동안 검색서비스 정지 불가피 | 신·구 인덱스를 가상 공간상에 병행 구동 → A/B 품질 검증 완료 후 동적 무중단 스위칭 |

---

### □ 임베딩 모델 무중단 전환 절차

① 신규 모델 선정·평가  
② GPU 유휴 시간대 배치 재임베딩  
③ 신·구 인덱스 병행 구동  
④ 검색 품질 비교·검증  
⑤ 단계적 전환(구 인덱스 정리)

⇒ **전환 비용의 성격 :** 시스템 재구축(×) → 배치 재처리 + 품질 검증(○)
