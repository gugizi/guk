# 기술독립성 위험 검토결과 (용어·표현 정비본)

> 원문 검토결과의 의미를 유지하되, 전문용어는 쉬운 말로 풀어쓰고  
> 문장 형식·종결을 통일하여 전달력을 높인 정비본입니다.  
> (문체 기준: □/○ 체계, `*` 용어풀이, 원인→대체가능성 서술)

---

## 용어 풀이 (본문 공통)

| 용어 | 쉬운 의미 |
|---|---|
| 기술독립성 위험 | 특정 제품·업체에 묶여 다른 제품으로 바꾸기 어려운 정도 |
| 벤더 | 솔루션을 공급하는 업체 |
| 오픈소스 | 소스코드를 공개해 자유롭게 사용·교체할 수 있는 소프트웨어 |
| 표준 인터페이스/규격 | 제품이 달라도 같은 방식으로 연결·교체할 수 있게 하는 약속된 형식 |
| 결합도(밀결합) | 시스템끼리 얼마나 깊게 묶여 있는지. 높을수록 한쪽만 바꾸기 어려움 |
| 락인(종속) | 특정 기술·업체에 고착되어 전환 비용이 커지는 상태 |
| 마이그레이션 | 기존 시스템·데이터를 다른 제품으로 옮겨가는 작업 |
| 벡터DB | 문서를 수치(벡터)로 저장하고, 의미가 비슷한 문서를 빠르게 찾는 데이터베이스 |
| pgvector | PostgreSQL에서 벡터 유사검색을 지원하는 오픈소스 확장 기능 |
| 재색인(Re-index) | 보존된 원문·정제데이터를 새 저장 형식에 맞게 다시 인덱스로 만드는 작업 |

※ 위험등급: **상**(높음) / **중**(보통) / **하**(낮음)

---

## □ 기술독립성 위험 검토결과

### 1. 사용자 인터페이스 (User Interface)

○ **(GUI Tool)** 위험: **하**  
- 화면 구성 기술이 표준화·일반화되어 있어, 특정 업체 제품에 묶이지 않음  
- 범용 라이브러리\*로 쉽게 교체 가능  
\* 라이브러리: 화면·기능을 만들 때 재사용하는 공용 프로그램 묶음

○ **(HTML5)** 위험: **하**  
- 국제 웹 표준 규격을 따르는 기술로, 특정 기기·브라우저·업체에 의존하지 않음  
- 표준을 지키는 다른 구현으로 독립적으로 교체 가능

---

### 2. 애플리케이션 서비스 (Application Service)

○ **(OCR)** 위험: **하**  
\* OCR(Optical Character Recognition): 이미지·스캔 문서의 글자를 인식하는 기술  
- 문자인식 솔루션이 시장에 다양하고, 표준 연결방식(인터페이스)을 따르면 제품 간 교체가 용이함

○ **(STT)** 위험: **중**  
\* STT(Speech to Text): 음성을 글자로 바꾸는 기술  
- 세무 전문용어 학습 등 국세행정 특화 작업이 일부 필요해 현재 제품과의 결합이 존재함  
- 다만 대체 가능한 공급업체가 다수 경쟁하고 있어 교체 자체는 가능함

○ **(TTS)** 위험: **중**  
\* TTS(Text to Speech): 글자를 음성으로 읽어주는 기술  
- 납세자 안내용 고품질 음성 조정이 필요함  
- 시장에 경쟁력 있는 대체 솔루션 공급자가 다수 존재함

○ **(검색엔진)** 위험: **하**  
- 검색 시스템을 다른 제품으로 옮기는 도구(마이그레이션)와 상용·오픈소스 대체재가 보편화됨  
- 전환 장벽이 낮아 교체가 용이함

○ **(지식그래프)** 위험: **중**  
\* 지식그래프: 세무 지식·관계를 연결해 저장·활용하는 기술  
- 세무 도메인 지식 구축·의미 연결 과정에 노하우 종속 우려가 있음  
- 다만 상용·오픈소스 기술이 일반화되어 있어 대체는 가능함

○ **(시각화)** 위험: **중**  
- 리포팅 도구와 연동할 때 일부 전용 규격에 묶일 수 있음  
- 표준 차트 엔진·웹 표준 기술을 쓰면 다른 제품으로 교체 가능함

○ **(텍스트 전처리)** 위험: **중**  
\* 텍스트 전처리: 검색·분석 전에 한글 문장을 쪼개고 정리하는 과정(형태소 분석 등)  
- 한글 분석 규칙이 세분화되면 특정 도구에 종속될 수 있음  
- 보편적인 오픈소스 한글 패키지로 전환 가능함

○ **(RPA)** 위험: **중**  
\* RPA(Robotic Process Automation): 반복 업무를 자동화하는 소프트웨어 로봇  
- 기존 업무절차와 연동 시 시나리오 수정 부담이 있음  
- 국내외 다수 업체가 경쟁하는 구조라 제품 교체는 용이함

○ **(ETL)** 위험: **하**  
\* ETL(Extract·Transform·Load): 데이터를 추출·변환·적재하는 연계 처리  
- 데이터 연계가 표준 통신방식(프로토콜)을 사용함  
- 업체 제약 없이 오픈소스나 다른 상용 도구로 즉시 교체 가능함

---

### 3. 애플리케이션 지원 서비스 (Application Support Service)

○ **(API Gateway)** 위험: **하**  
\* API Gateway: 외부·내부 서비스 호출을 한곳에서 받아 인증·경로·트래픽을 통제하는 관문  
- 트래픽 제어·인증·경로지정(라우팅)이 표준 기술 기반으로 설계됨  
- 범용 제품 또는 자체 오픈소스 프레임워크로 즉시 교체 가능함

○ **(WEB / WAS)** 위험: **하**  
\* WEB: 웹 서버 / WAS(Web Application Server): 업무 프로그램을 실행하는 서버  
- 국내외 상용(JEUS 등)·오픈소스 제품군이 표준 기술 규격을 준수함  
- 제품 간 상호 교체가 즉시 가능함

---

### 4. 데이터 서비스 (Data Service)

○ **(DBMS)** 위험: **하**  
\* DBMS: 데이터베이스를 관리하는 시스템  
- 관계형 DB의 표준 SQL을 준수함  
- PostgreSQL 등 신뢰성 높은 오픈소스 대체재가 시장에 풍부함

---

### 5. 애플리케이션 개발환경 (Application Development Environment)

○ **(웹개발도구)** 위험: **하**  
- 전자정부 표준 프레임워크 기반의 범용 개발 규격을 따름  
- 특정 개발도구에 대한 의존성이 없음

---

### 6. 시스템 플랫폼 (System Platform) ※ 위험 ‘상’ 집중 구간

○ **(LLM)** 위험: **상**  
\* LLM(Large Language Model): 대규모 언어모델(질의 응답·문서 생성 등)  
- 특정 업체 API\*에만 의존할 경우 서비스 정책 변경·중단 위험이 가장 큼  
\* API: 프로그램끼리 기능을 주고·응답하는 연결 창구  
- **해소방향:** 표준 REST\* 방식 호출 + 개방형 가중치 모델(Llama 등)을 기관 내부에 두는 구조로 대체 경로 확보  
\* REST: 웹에서 널리 쓰는 표준적인 요청·응답 방식  
※ 상세: 추상화 중계 레이어·OpenAI 호환 API로 모델 교체 가능 (별도 검토서)

○ **(RAG)** 위험: **상**  
\* RAG(Retrieval-Augmented Generation): 내부 문서를 찾아 답변에 반영하는 방식  
- 프레임워크와 지식저장소가 깊게 묶이면(밀결합) 한쪽만 바꾸기 어려움  
- **해소방향:** 모듈형 데이터 파이프라인\* 설계로 이후 이전·재구성 가능  
\* 모듈형 파이프라인: 파싱·분류·청킹·임베딩·인덱스 단계를 분리해, 바뀌는 단계만 다시 수행하는 구조  
※ 상세: 임베딩 교체 시 원문 재수집 없이 재임베딩·무중단 전환 (별도 검토서)

○ **(벡터DB)** 위험: **상**  
\* 벡터DB: 문서를 수치(벡터)로 저장해 의미가 비슷한 문서를 빠르게 찾는 데이터베이스  
- 제품별 전용 색인·저장 형식과 전용 검색 API에 묶이면, 제품 교체 시 인덱스 재구축과 응용코드 수정이 함께 발생함  
- 임베딩 차원·거리함수(유사도 계산 방식)가 제품·모델마다 달라 단순 데이터 이전이 어려움  
- **해소방향:** 응용은 표준 검색(Retriever) API만 호출하고, 하위 저장소는 YAML 설정으로 교체  
- **해소방향:** 원문·정제데이터는 데이터레이크에 보존하고, 벡터 인덱스는 파생 산출물로 관리하여 재색인만 수행  
- **해소방향:** PostgreSQL(pgvector) 등 오픈소스 스택을 기본 전환 경로로 확보하고, 초대규모 검색만 전용 벡터DB를 선택 연결  
※ 상세: 검색 인터페이스 표준화·이중 적재·무중단 전환 (별도 검토서)

---

### 7. 네트워크 (Network)

○ **(WAN)** 위험: **하**  
\* WAN: 원격 거점을 잇는 광역 통신망  
- 표준 통신규격 기반의 전용회선 인프라임  
- 국내 기간통신사업자가 다수 있어 사업자 변경이 자유로움

---

### 8. 보안 및 시스템 관리 (Security & System Management)

○ **(정보보호)** 위험: **하**  
- 국가 정보보안 지침 및 CC\* 인증 등 표준 기준을 따름  
\* CC(Common Criteria): 정보보호제품 평가·인증의 국제 공통 기준  
- 검증된 공공 보안 솔루션으로 상호 교체 가능함

---

### 9. 외부접근 환경 (External Access Environment)

○ **(망연계)** 위험: **중**  
\* 망연계: 내부망과 외부망을 물리적으로 분리한 뒤, 허용된 방식으로만 연결하는 체계  
- 망 분리에 따른 연계 설계 제약이 있음  
- 공공 보안 검증을 마친 국산 망연계 솔루션이 다수 있어 대체 가능함

---

## □ 정비 요약 (표현·용어 원칙)

○ **(문장 형식)** “위험·제약 요인 → 대체·독립 가능성” 순으로 통일  
○ **(종결)** “~함 / ~가능함 / ~존재함”으로 문체 일치 (원문의 “~니다/~음” 혼용 해소)  
○ **(용어)** 락인→종속, 밀결합→깊게 묶임(밀결합), 이식 장벽→전환 장벽, API 폐쇄화→특정 업체 API 의존, 대납세자→납세자 등 의미 전달 우선  
○ **(약어)** OCR·STT·TTS·RPA·ETL·LLM·RAG·WAS·CC 등은 첫 등장 시 쉬운 풀이 병기  
○ **(상 등급)** LLM·RAG·벡터DB는 위험 원인과 해소방향을 구분해, 선행 검토서(비종속 아키텍처·모듈형 파이프라인)와 연결

---

## □ 원문 대비 주요 수정 예시

| 구분 | 원문 표현 | 정비 표현 |
|---|---|---|
| LLM | API 폐쇄화 위험, 오픈 가중치 모델 내재화 | 특정 업체 API 의존 위험, 개방형 모델을 기관 내부에 두는 구조 |
| RAG | 밀결합 리스크, 이식 및 재구축 | 깊게 묶이면 교체가 어려움, 이전·재구성 |
| 벡터DB | 전용 인덱싱 및 저장 규격 종속, 대체 오픈소스 스택 | 전용 색인·검색 API에 묶일 우려, PostgreSQL(pgvector)로 전환 경로 확보 |
| 검색엔진 | 이식 장벽이 낮음 | 전환 장벽이 낮아 교체가 용이함 |
| 텍스트 전처리 | 락인이 발생할 수 있으나 | 특정 도구에 종속될 수 있으나 |
| TTS | 대납세자 안내 | 납세자 안내 |
| 공통 | ~가능합니다 / ~가능 / ~있음 혼용 | ~가능함 / ~존재함 등으로 통일 |
